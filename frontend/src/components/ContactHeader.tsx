import { company } from '../data/products';
export default function ContactHeader(){return <div className="top-strip"><div className="site-container top-strip-inner"><div className="top-message">YOUR TRUSTED PARTNER FOR IT, POWER BACKUP & SECURITY SOLUTIONS</div><div className="top-actions"><a href={company.phoneHref}>{company.phone}</a><a href={company.emailHref}>{company.email}</a><a className="top-whatsapp" href={company.whatsappHref} target="_blank" rel="noreferrer">WhatsApp</a></div></div></div>}
